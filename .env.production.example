# Frontend production environment
# Safe to expose in the browser bundle.
# Do not put API keys or secrets in VITE_* values.

# Leave empty when frontend and backend are served from same domain.
VITE_API_BASE_URL=

# Optional weather proxy base. Leave empty to use same-origin /api routes.
VITE_WEATHER_PROXY_BASE=
